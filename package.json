{
  "name": "flumelog-array",
  "version": "1.2.0",
  "description": "like flumelog-memory without the filesystem",
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "obv": "0.0.1",
    "pull-cursor-array": "^2.0.0"
  },
  "devDependencies": {
    "test-flumelog": "^1.0.0"
  }
}
